import 'package:flutter/material.dart';

class LabReport extends StatelessWidget {
  const LabReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Lab Reprts"),),
    );
  }
}